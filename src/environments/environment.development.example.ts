export const environment = {
  production: false,
  // Base URL of the Ventura backend. Generated into environment.ts /
  // environment.development.ts by set-env.js from the API_URL env var.
  apiUrl: 'https://dev.ventura.csniico.com',
  // Google Identity Services web client id (for Google sign-in idToken).
  googleClientId: 'WEB_GOOGLE_CLIENT_ID_HERE',
};
